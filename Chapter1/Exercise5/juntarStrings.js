// Escreva uma função que junta duas strings.

function juntarStrings(s1, s2) {
    return s1 +s2 ; 
    // ou gpt : return s1.concat(s2); 
}

console.log("Strings juntas:", juntarStrings("Hello, ", "World!"));
